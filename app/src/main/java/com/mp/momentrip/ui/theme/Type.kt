package com.mp.momentrip.ui.theme

import androidx.compose.material3.Typography
import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.unit.sp
import com.mp.momentrip.R

val Typography = Typography(
    displayLarge = TextStyle(
        fontFamily = FontFamily(Font(R.font.pretendar)),
        fontWeight = FontWeight.Normal,
        fontSize = 64.sp // 기존 57.sp
    ),
    displayMedium = TextStyle(
        fontFamily = FontFamily(Font(R.font.pretendar)),
        fontWeight = FontWeight.Normal,
        fontSize = 52.sp // 기존 45.sp
    ),
    displaySmall = TextStyle(
        fontFamily = FontFamily(Font(R.font.pretendar)),
        fontWeight = FontWeight.Normal,
        fontSize = 40.sp // 기존 36.sp
    ),
    headlineLarge = TextStyle(
        fontFamily = FontFamily(Font(R.font.pretendar)),
        fontWeight = FontWeight.Normal,
        fontSize = 36.sp // 기존 32.sp
    ),
    headlineMedium = TextStyle(
        fontFamily = FontFamily(Font(R.font.pretendar)),
        fontWeight = FontWeight.Normal,
        fontSize = 32.sp // 기존 28.sp
    ),
    headlineSmall = TextStyle(
        fontFamily = FontFamily(Font(R.font.pretendar)),
        fontWeight = FontWeight.Normal,
        fontSize = 28.sp // 기존 24.sp
    ),
    titleLarge = TextStyle(
        fontFamily = FontFamily(Font(R.font.pretendar)),
        fontWeight = FontWeight.Normal,
        fontSize = 26.sp // 기존 22.sp
    ),
    titleMedium = TextStyle(
        fontFamily = FontFamily(Font(R.font.pretendar)),
        fontWeight = FontWeight.Medium,
        fontSize = 20.sp // 기존 16.sp
    ),
    titleSmall = TextStyle(
        fontFamily = FontFamily(Font(R.font.pretendar)),
        fontWeight = FontWeight.Medium,
        fontSize = 15.sp // 기존 14.sp
    ),
    bodyLarge = TextStyle(
        fontFamily = FontFamily(Font(R.font.pretendar)),
        fontWeight = FontWeight.Normal,
        fontSize = 20.sp // 기존 16.sp
    ),
    bodyMedium = TextStyle(
        fontFamily = FontFamily(Font(R.font.pretendar)),
        fontWeight = FontWeight.Normal,
        fontSize = 20.sp // 기존 14.sp
    ),
    bodySmall = TextStyle(
        fontFamily = FontFamily(Font(R.font.pretendar)),
        fontWeight = FontWeight.Normal,
        fontSize = 16.sp // 기존 12.sp
    ),
    labelLarge = TextStyle(
        fontFamily = FontFamily(Font(R.font.pretendar)),
        fontWeight = FontWeight.Medium,
        fontSize = 18.sp // 기존 14.sp
    ),
    labelMedium = TextStyle(
        fontFamily = FontFamily(Font(R.font.pretendar)),
        fontWeight = FontWeight.Medium,
        fontSize = 16.sp // 기존 12.sp
    ),
    labelSmall = TextStyle(
        fontFamily = FontFamily(Font(R.font.pretendar)),
        fontWeight = FontWeight.Medium,
        fontSize = 14.sp // 기존 11.sp
    )
)
